{
  "name": "@homekeeper/ui",
  "version": "0.1.0",
  "private": true,
  "description": "HomeKeeper MCP App views (single-file HTML cards rendered by Alexa+ / any MCP Apps host)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "vite --config vite.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.5"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vite-plugin-singlefile": "^2.2.0"
  }
}
